'use client';

// Counsel uses a top-nav layout, not a sidebar. This component is kept as a
// thin stub so any lingering imports/tests continue to compile cleanly. If a
// future direction needs a sidebar again, build it here.
export default function Sidebar(_: { onNewDocument: () => void }) {
  return null;
}
